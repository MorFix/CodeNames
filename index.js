/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StatusDisplay from './src/StatusDisplay/StatusDisplay';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StatusDisplay);
